# LOCAL DEVELOPMENT ONLY
server.port=8090
spring.application.name=altius-academy-backend

# MySQL Local
spring.datasource.url=jdbc:mysql://127.0.0.1:3306/AltiusV3?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.driverClassName=com.mysql.cj.jdbc.Driver
spring.datasource.username=root
spring.datasource.password=120994

# JPA
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Mongo Local
spring.data.mongodb.uri=mongodb://localhost:27017/altiusV3

# JWT
app.jwtSecret=altiusAcademySecretKey2025VerySecureKeyForDevelopmentOnly
app.jwtExpirationInMs=86400000

# Email mock
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=test@example.com
spring.mail.password=test
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# CORS
app.cors.allowed-origins=http://localhost:3000,http://localhost:5173
app.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS,PATCH
app.cors.allowed-headers=*
app.cors.allow-credentials=true

# Uploads
file.upload-dir=uploads

# Logging
logging.level.com.altiusacademy=DEBUG
